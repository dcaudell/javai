package dev.xtrafe.javai.tagging;

import dev.xtrafe.javai.persistence.JavAIRepository;

/** Ordinary realized repository for {@link TestTextedThing} -- same pattern as {@link TestThingRepository}. */
public interface TestTextedThingRepository extends JavAIRepository<TestTextedThing> {
}
