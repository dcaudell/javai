package dev.xtrafe.javai.persistence;

/** Repository for OMI-254's optimistic-locking fixture -- see {@link TestVersionedAsset}. */
interface TestVersionedAssetRepository extends JavAIRepository<TestVersionedAsset> {
}
