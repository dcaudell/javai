package dev.xtrafe.javai.persistence;

/** Repository for OMI-254's non-vectorized optimistic-locking fixture -- see {@link TestVersionedCounter}. */
interface TestVersionedCounterRepository extends JavAIRepository<TestVersionedCounter> {
}
