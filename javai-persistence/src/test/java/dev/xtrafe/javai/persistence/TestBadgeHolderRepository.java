package dev.xtrafe.javai.persistence;

/** Repository over the bidirectional/non-optional {@code @OneToOne} fixture. */
interface TestBadgeHolderRepository extends JavAIRepository<TestBadgeHolder> {
}
