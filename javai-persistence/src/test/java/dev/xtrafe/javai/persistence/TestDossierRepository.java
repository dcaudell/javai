package dev.xtrafe.javai.persistence;

/** Repository for {@link TestDossier}, so it can be persisted independently of its container. */
interface TestDossierRepository extends JavAIRepository<TestDossier> {
}
