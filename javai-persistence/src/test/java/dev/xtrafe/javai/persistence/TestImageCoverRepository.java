package dev.xtrafe.javai.persistence;

/** Exists only so OMI-214's tests can ask for a repository of a type registered purely by discovery. */
interface TestImageCoverRepository extends JavAIRepository<TestImageCover> {
}
